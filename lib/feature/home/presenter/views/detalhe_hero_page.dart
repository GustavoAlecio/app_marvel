import 'package:flutter/material.dart';

class DetalhesHeroPage extends StatelessWidget {
  const DetalhesHeroPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
